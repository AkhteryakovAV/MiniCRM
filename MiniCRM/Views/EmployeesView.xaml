<Window x:Class="MiniCRM.Views.EmployeesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniCRM.Views"
        xmlns:vm="clr-namespace:MiniCRM.PresentationLogic.ViewModels;assembly=MiniCRM.PresentationLogic"
        d:DataContext="{d:DesignInstance Type=vm:EmployeesViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="Сотрудники" Height="500" Width="350"
        Icon="{DynamicResource AppIcon}" WindowStartupLocation="CenterScreen"
        >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Templates/EmployeeTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel>
        <Button DockPanel.Dock="Top"
                Content="Новый сотрудник"
                HorizontalAlignment="Left"
                Command="{Binding CreateEmployeeCommand}"
                />
        <ListView ItemsSource="{Binding Employees}"
                  ItemTemplate="{StaticResource EmployeeItemTemplate }"
                  HorizontalContentAlignment="Stretch"
                  />

    </DockPanel>
</Window>
